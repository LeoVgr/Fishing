// Each #kernel tells which function to compile; you can have many kernels
#pragma kernel CSMain

struct MeshProperty
{
    float4x4 mat;
};

RWStructuredBuffer<MeshProperty> meshProperties;

[numthreads(8,8,1)]
void CSMain (uint3 id : SV_DispatchThreadID)
{
    //meshProperties[id.x, id.y].mat = float4x4(0,0);
}
